# ยอมรับเงื่อนไขเครื่องยนต์ (Engine) เพื่อป้องกันปัญหาในอนาคต
engine-strict=true

# บันทึกความปลอดภัยสิทธิ์การรันสคริปต์เบื้องหลัง (เผื่อสำหรับเครื่องที่ใช้ npm เวอร์ชันใหม่)
# หมายเหตุ: นโยบายรายแพ็คเกจแบบนี้ แนะนำให้ใส่เพิ่มใน "allowScripts" ของ package.json ควบคู่กันไป
